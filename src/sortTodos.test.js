import { describe, test, expect } from "vitest";

describe("sortTodos", () => {
  describe("期限でのソート", () => {
    test("期限の昇順でソートされること", () => {
      // テストを通すための仮実装
      expect(1).toBe(1);
    });
    test("期限の降順でソートされること", () => {
      // テストを通すための仮実装
      expect(1).toBe(1);
    });
    test("うるう年でも正しくソートされること", () => {
      // テストを通すための仮実装
      expect(1).toBe(1);
    });
    test("同じ期限の時はid順でソートされること", () => {
      // テストを通すための仮実装
      expect(1).toBe(1);
    });
  });

  describe("タイトルでのソート", () => {
    test("タイトルの昇順でソートされること", () => {
      // テストを通すための仮実装
      expect(1).toBe(1);
    });
    test("タイトルの降順でソートされること", () => {
      // テストを通すための仮実装
      expect(1).toBe(1);
    });
    test("同じタイトルの時はid順でソートされること", () => {
      // テストを通すための仮実装
      expect(1).toBe(1);
    });
  });
});
